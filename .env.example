# Google OAuth Client ID for Google Drive sync.
# Create one at https://console.cloud.google.com/
# (enable the Drive API and add your app's URL as an authorized JavaScript origin).
# Leave unset to hide the Google Drive sync UI entirely.
PUBLIC_GOOGLE_CLIENT_ID=
